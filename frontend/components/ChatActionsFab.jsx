// Neutralized ChatActionsFab (undo): no-op component left here to avoid import errors.
import React from 'react';
import { View } from 'react-native';

export default function ChatActionsFab() {
  return <View />;
}